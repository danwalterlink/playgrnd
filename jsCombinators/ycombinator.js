// yCombintorial solution for n^n
let y;
y => f(y);
  y = le =>
  { f =>
    { f(f)
    }; f =>
    { le(
      n => (f(f)(n*n))
      );
    };
  };
)

/**
 * in simple form:
 */
let variable = console.log('hi');
function someFunction(v) {

}

/**
 * the mockingbird in javascript
 */

 let mockingbird = fn => (...args) => fn(fn, ...args)
// cute, but does not compile.
// this should

y = (...le) => x => le.reduce((f, n) => f(n*n), x)

/**
 * the overall idea is to wrap a function in a higher-order function
 * that calls the function
 */